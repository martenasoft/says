{% extends 'base.html.twig' %}

{% block body %}

    {{ include('page/section_element_without_links.html.twig') }}

    {{ include('feedback/_form.html.twig') }}

   {{ page.body is defined ? page.body }}
{% endblock %}