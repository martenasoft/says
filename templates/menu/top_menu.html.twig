<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-2">
    <div class="container-fluid">
        <a class="navbar-brand" href="/{{ app.request.locale }}">Logo</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                {% for item in items %}
                    {% include 'menu/menu_link.html.twig' with { linkClass: 'nav-link' } %}
                {% endfor %}

            </ul>
            <ul class="navbar-nav ms-auto">
                {% include 'elements/lang_switcher.html.twig' with { activeRequest: activeRequest } %}
                {% if app.user is not null %}
                    <li class="nav-item">
                        <a class="nav-link " href="{{ path('app_user_profile', {id: app.user.id}) }}">{{ 'Profile' | trans }}: {{ app.user.email}}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">{{ 'Logout' | trans }}</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">{{ 'Login' | trans }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">{{ 'Registration' | trans }}</a>
                    </li>
                {% endif %}
            </ul>

        </div>
    </div>
</nav>