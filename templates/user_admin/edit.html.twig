{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {{ include('user/tabs.html.twig', ({items: [
        {'route' : 'app_user_admin_edit', 'params': {'id': user.id}, 'title': 'Default'},
        {'route' : 'app_user_profile_change_password', 'params': {'id': user.id}, 'title': 'Security'},

    ]})) }}

    {{ include('user_admin/_form.html.twig') }}

    {{ include('elements/form_bottom_buttons.html.twig',{
        'back_to_list_path':  path('app_user_admin_index'),
        'delete_form_file_name' : 'user_admin/_delete_form.html.twig',
        'form_id': 'user-form',
        'button_label' : 'Update'
    }
    ) }}
{% endblock %}
