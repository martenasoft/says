{% extends 'base.html.twig' %}

{% block title %}{{ 'Permission' | trans }}{% endblock %}

{% block body %}
    <h1>Permission</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>{{ 'Id' | trans }}</th>
                <td>{{ permission.id }}</td>
            </tr>
            <tr>
                <th>{{ 'Name' | trans }}</th>
                <td>{{ permission.name }}</td>
            </tr>
        </tbody>
    </table>
    {{ include('elements/form_bottom_buttons.html.twig',{
        'back_to_list_path':  path('app_permission_admin_index'),
        'edit_path':  path('app_role_admin_edit', {id: permission.id}),
        'delete_form_file_name' : 'permission_admin/_delete_form.html.twig'
    }
    ) }}
{% endblock %}
