<form method="post" action="{{ path('app_role_admin_delete', {'id': role.id}) }}" onsubmit="return confirm('{{ 'Are you sure you want to delete this item?' | trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ role.id) }}">
    <button class="btn">{{ 'Delete' | trans }}</button>
</form>
