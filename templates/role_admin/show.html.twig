{% extends 'base.html.twig' %}

{% block title %}Role{% endblock %}

{% block body %}
    <h1>Role</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ role.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ role.name }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('elements/form_bottom_buttons.html.twig',{
        'back_to_list_path':  path('app_role_admin_index'),
        'edit_path':  path('app_role_admin_edit', {id: role.id}),
        'delete_form_file_name' : 'role_admin/_delete_form.html.twig'
    }
    ) }}
{% endblock %}
